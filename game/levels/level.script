local data = require "main.data"

function init(self)
	local s = 30
	local x = {s, s*2, s*3, data.CANV_W - s,data.CANV_W - s * 2,data.CANV_W - s * 3}

	for n = 1, 6 do
		local pos = vmath.vector3(x[n], 20, 0)
		factory.create("#city-fac", pos)
	end
end
